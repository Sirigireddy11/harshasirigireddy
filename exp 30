#include<stdio.h>
#include<fcntl.h>
#include<unistd.h>
int main()
{
int fd;
char buf[50];
fd=open("file.txt",O_CREAT|O_RDWR,0666);
write(fd,"Hello",5);
lseek(fd,0,SEEK_SET);
read(fd,buf,5);
buf[5]='\0';
printf("%s",buf);
close(fd);
return 0;
}
